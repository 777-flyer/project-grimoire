# Grimoire — Frontend

Next.js (App Router) client for Grimoire. See the [repository root README](../README.md) for the project overview, architecture, and setup instructions covering both the backend and frontend.

```bash
npm install
npm run dev
```

Runs at `http://localhost:3000`, talking to the Django API at `http://localhost:8000`.
